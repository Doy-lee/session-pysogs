[log]
; Log level for the plugin (DEBUG, INFO, WARNING, ERROR, CRITICAL)
; Can be overridden per-plugin in [plugin_emoji_captcha] as 'log_level'
level = INFO

[plugin]
; Configuration for the plugin connection to the SOGS server

; The public key of your SOGS. This can be found in your room URLs.
; For example, if the URL to join your room was http://xx.xx.xx.xx/roomname?public_key=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
; your sogs_pubkey_hex would be sogs_pubkey_hex = xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
sogs_pubkey_hex = xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

; The SOGS OMQ address which plugins will operate against.
sogs_address = tcp://127.0.0.1:22028

[plugin_emoji_captcha]
; The display name of the CAPTCHA plugin that will be displayed to Session clients when messaged
; with the challenge.
name = Emoji CAPTCHA Plugin

; Path to the ed25519 private key file. This is a 64-byte file containing the raw private key data.
; A new key is generated by default if a key is not found at this location.
key_file = plugin_emoji_captcha_ed25519

; Log level for this plugin (overrides [log] section if set)
log_level = INFO

; This is the total number of unique CAPTCHAs the SOGS will provide to the user if they fail a
; CAPTCHA or refresh the CAPTCHA. If the user exceeds the retry limit, they will need to contact
; the SOGS operator to have their permissions manually updated.
captcha_limit = 3

; This is the number of seconds the SOGS will wait after a user successfully solves a CAPTCHA before
; the user is given write permissions.
write_timeout_s = 120

; This is the number of seconds the SOGS will wait before allowing a user to refresh the CAPTCHA.
refresh_timeout_s = 60

; This is the number of seconds the SOGS will wait before providing the next CAPTCHA if a user fails
; the CAPTCHA.
retry_timeout_s = 60

; Set the path to a text file that contains the list of emojis to use in the CAPTCHA challenge. You
; may specify the emojis using a unicode escaped string, or otherwise the emoji verbatim. Comments
; are supported by suffixing the line with '#' followed by the comment.
;
; You may use \U syntax for 8 byte, \u for 4 byte encoding of emojis or the emoji glyph directly.
; The specified path can be absolute or relative. If it's relative, the file is opened relative to
; the directory that this .INI file is loaded from. If this field is not supplied, a default list of
; emojis will be used.
;
; Example:
;
;   \U0001F602 # 😂
;   ❤️
;
; Would make the plugin generate challenge CAPTCHAs with an answer of either the crying-face or
; heart emoji. Duplicate emojis increases the chance of the emoji being selected.
; emoji_list_file = <path/to/file.txt>
